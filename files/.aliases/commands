# coloured ls
if [ "$TERM" != "dumb" ]; then
  color_option='--color=auto'
  alias ls="ls $color_option"
fi

alias l="ls"
alias ll="ls -lh"
alias la="ls -a"
alias lal="ls -lha"

alias less="less -R"
alias psf="ps faux"

alias m="sudo monit"
alias ms="sudo monit status"

alias m='mate .'
